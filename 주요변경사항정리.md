# 주요 변경사항 정리

## 10단계: 게시글 목록 검색 기능

### 개요
사용자가 원하는 게시글을 빠르게 찾을 수 있도록 제목과 내용에서 검색할 수 있는 기능을 추가했습니다.

---

## 📋 목차 (빠른 이동)

- [1. 검색의 원리 (Database & Repository)](#1-검색의-원리-database--repository)
  - [LIKE 검색 개념](#like-검색-개념)
  - [BoardRepository 변경사항](#boardrepository-변경사항)
- [2. 검색 로직 처리 (Service)](#2-검색-로직-처리-service)
  - [BoardService 변경사항](#boardservice-변경사항)
- [3. 검색 파라미터 처리 (Controller)](#3-검색-파라미터-처리-controller)
  - [BoardController 변경사항](#boardcontroller-변경사항)
- [4. 검색 UI 구현 (View)](#4-검색-ui-구현-view)
  - [list.mustache 변경사항](#listmustache-변경사항)
- [💡 학생들을 위한 개념 설명](#-학생들을-위한-개념-설명)
- [주요 파일 변경 목록](#주요-파일-변경-목록)
- [테스트 시나리오](#테스트-시나리오)

---

## 1. 검색의 원리 (Database & Repository)

### LIKE 검색 개념

**"사용자가 '스프링'이라고 검색하면, 제목이나 내용에 '스프링'이 포함된 모든 게시글을 찾아야 합니다."**

데이터베이스에서 문자열을 검색할 때는 **LIKE 연산자**를 사용합니다.

**LIKE 패턴**:
- `%keyword%`: 앞뒤에 아무 문자나 와도 됨 (부분 일치)
  - 예: `%스프링%` → "스프링", "스프링부트", "자바스프링", "스프링강의" 모두 매칭
- `keyword%`: 앞에만 아무 문자나 와도 됨 (접두사 일치)
- `%keyword`: 뒤에만 아무 문자나 와도 됨 (접미사 일치)

**대소문자 구분 없이 검색**:
- `LOWER()` 함수를 사용하여 대소문자 구분 없이 검색
- 예: "Spring", "SPRING", "spring" 모두 동일하게 처리

### BoardRepository 변경사항

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardRepository.java`

```java
/**
 * 게시글 검색 (제목 또는 내용, 페이징 포함)
 * 
 * LIKE 검색:
 * - 제목(title) 또는 내용(content)에 검색어가 포함된 게시글 조회
 * - 대소문자 구분 없이 검색 (LOWER 함수 사용)
 * - %keyword% 형태로 부분 일치 검색
 */
@Query(value = "SELECT DISTINCT b FROM Board b JOIN FETCH b.user " +
       "WHERE LOWER(b.title) LIKE LOWER(CONCAT('%', :keyword, '%')) " +
       "   OR LOWER(b.content) LIKE LOWER(CONCAT('%', :keyword, '%')) " +
       "ORDER BY b.createdAt DESC",
       countQuery = "SELECT COUNT(DISTINCT b) FROM Board b " +
       "WHERE LOWER(b.title) LIKE LOWER(CONCAT('%', :keyword, '%')) " +
       "   OR LOWER(b.content) LIKE LOWER(CONCAT('%', :keyword, '%'))")
Page<Board> findByTitleContainingOrContentContaining(@Param("keyword") String keyword, Pageable pageable);
```

**코드 설명**:
- `LOWER(b.title) LIKE LOWER(CONCAT('%', :keyword, '%'))`
  - `LOWER()`: 대소문자 구분 없이 검색
  - `CONCAT('%', :keyword, '%')`: 검색어 앞뒤에 `%` 추가하여 부분 일치 검색
  - 예: 검색어가 "스프링"이면 `%스프링%`로 변환

- `OR LOWER(b.content) LIKE ...`
  - 제목 또는 내용 중 하나라도 검색어가 포함되면 조회

- `JOIN FETCH b.user`
  - 작성자 정보도 함께 조회 (N+1 문제 방지)

- `countQuery`
  - 검색 결과의 전체 개수를 정확하게 계산하기 위한 별도 쿼리

**생성되는 SQL**:
```sql
-- 데이터 조회 쿼리
SELECT DISTINCT b.*, u.* 
FROM board_tb b 
INNER JOIN user_tb u ON b.user_id = u.id 
WHERE (LOWER(b.title) LIKE LOWER('%스프링%') 
   OR LOWER(b.content) LIKE LOWER('%스프링%'))
ORDER BY b.created_at DESC
LIMIT 5 OFFSET 0

-- 개수 조회 쿼리 (countQuery)
SELECT COUNT(DISTINCT b) 
FROM board_tb b 
WHERE (LOWER(b.title) LIKE LOWER('%스프링%') 
   OR LOWER(b.content) LIKE LOWER('%스프링%'))
```

---

## 2. 검색 로직 처리 (Service)

### BoardService 변경사항

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardService.java`

```java
/**
 * 게시글 목록 조회 (페이징, 검색 포함)
 * 
 * @param page 페이지 번호 (0부터 시작)
 * @param size 페이지 크기
 * @param keyword 검색어 (제목 또는 내용, null 가능)
 * @return 페이징된 게시글 목록 DTO
 */
@Transactional(readOnly = true)
public BoardResponse.PageDTO 게시글목록조회(int page, int size, String keyword) {
    // ... 페이징 처리 코드 ...
    
    // 검색어가 있으면 검색, 없으면 전체 조회
    Page<Board> boardPage;
    if (keyword != null && !keyword.trim().isEmpty()) {
        // 검색어가 있는 경우: 제목 또는 내용에서 검색
        boardPage = boardRepository.findByTitleContainingOrContentContaining(keyword.trim(), pageable);
    } else {
        // 검색어가 없는 경우: 전체 조회
        boardPage = boardRepository.findAllWithUserOrderByCreatedAtDesc(pageable);
    }
    
    return new BoardResponse.PageDTO(boardPage);
}
```

**코드 설명**:
- `keyword != null && !keyword.trim().isEmpty()`
  - 검색어가 null이 아니고, 공백만 있는 경우도 제외
  - `trim()`: 앞뒤 공백 제거

- 조건부 분기
  - 검색어가 있으면: 검색 쿼리 실행
  - 검색어가 없으면: 전체 조회 쿼리 실행

**핵심 포인트**:
- 검색어가 없을 때도 정상적으로 전체 목록을 보여줌
- 검색어 앞뒤 공백을 제거하여 정확한 검색 보장

---

## 3. 검색 파라미터 처리 (Controller)

### BoardController 변경사항

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardController.java`

```java
@GetMapping({"/board/list", "/"})
public String boardList(
        @RequestParam(defaultValue = "1") int page,
        @RequestParam(defaultValue = "5") int size,
        @RequestParam(required = false) String keyword,  // 검색어 파라미터 추가
        Model model) {
    int pageIndex = Math.max(0, page - 1);
    
    BoardResponse.PageDTO boardPage = boardService.게시글목록조회(pageIndex, size, keyword);
    
    // View에 데이터 전달
    // boardPage 객체에 페이징 정보와 게시글 목록이 모두 포함되어 있음
    // keyword도 전달하여 검색어 유지 (페이지 이동 시 검색어 유지)
    // keyword가 null이면 빈 문자열로 전달 (Mustache 템플릿 오류 방지)
    model.addAttribute("boardPage", boardPage);
    model.addAttribute("keyword", keyword != null ? keyword : "");
    return "board/list";
}
```

**코드 설명**:
- `@RequestParam(required = false) String keyword`
  - `required = false`: 검색어는 선택적 파라미터 (없어도 됨)
  - 검색어가 없으면 `null`이 됨

- `model.addAttribute("keyword", keyword != null ? keyword : "")`
  - 검색어를 View에 전달하여 검색 폼에 표시
  - 페이지 이동 시 검색어가 유지되도록 함
  - **중요**: `keyword`가 `null`이면 빈 문자열(`""`)로 전달
  - Mustache 템플릿에서 변수가 없을 때 발생하는 오류 방지

---

## 4. 검색 UI 구현 (View)

### list.mustache 변경사항

**파일**: `src/main/resources/templates/board/list.mustache`

```mustache
<!-- 검색 폼 -->
<div class="card mb-4">
    <div class="card-body">
        <form action="/board/list" method="GET" class="row g-3">
            <div class="col-md-8">
                <input type="text" 
                       class="form-control" 
                       name="keyword" 
                       placeholder="제목 또는 내용으로 검색..." 
                       value="{{#keyword}}{{keyword}}{{/keyword}}">
            </div>
            <div class="col-md-2">
                <input type="hidden" name="page" value="1">
                <input type="hidden" name="size" value="{{boardPage.size}}">
                <button type="submit" class="btn btn-primary w-100">검색</button>
            </div>
            <div class="col-md-2">
                <a href="/board/list" class="btn btn-secondary w-100">초기화</a>
            </div>
        </form>
    </div>
</div>
```

**코드 설명**:
- `action="/board/list"`: 게시글 목록 페이지로 요청 (홈 화면과 분리)
- `value="{{#keyword}}{{keyword}}{{/keyword}}"`: 
  - 검색어가 있을 때만 입력 필드에 표시 (검색어 유지)
  - `{{#keyword}}...{{/keyword}}`: Mustache 조건부 렌더링 (keyword가 있을 때만 표시)
  - Mustache 템플릿에서 변수가 없을 때 발생하는 오류 방지
- `method="GET"`: URL에 검색어가 포함되어 북마크 가능
- `input type="hidden"`: 페이지 번호와 크기를 함께 전달
- `초기화 버튼`: 검색어를 제거하고 전체 목록으로 이동 (`/board/list`)

**페이징 링크에 검색어 유지**:
```mustache
<a class="page-link" href="/board/list?page={{displayNumber}}&size={{boardPage.size}}{{#keyword}}&keyword={{keyword}}{{/keyword}}">
```

- `{{#keyword}}&keyword={{keyword}}{{/keyword}}`
  - 검색어가 있을 때만 URL에 추가
  - 페이지 이동 시에도 검색 결과 유지
- 모든 페이징 링크가 `/board/list` 기준으로 동작 (홈 화면과 분리)

---

## 💡 학생들을 위한 개념 설명

### 1. LIKE 검색이란?

**"네이버나 구글에서 검색할 때, 정확히 일치하는 것만 찾나요? 아니면 포함된 것도 찾나요?"**

- **정확 일치**: `WHERE title = '스프링'` → "스프링"만 찾음
- **부분 일치**: `WHERE title LIKE '%스프링%'` → "스프링", "스프링부트", "자바스프링" 모두 찾음

**실생활 예시**:
- 전화번호부에서 "김"을 검색하면 "김철수", "김영희", "김민수" 모두 나옴
- 이것이 LIKE 검색의 원리입니다!

### 2. 왜 LOWER()를 사용하나요?

**"Spring", "SPRING", "spring" - 이 세 개는 같은 건가요, 다른 건가요?"**

- 데이터베이스는 기본적으로 대소문자를 구분합니다
- `LOWER()` 함수로 모두 소문자로 변환하여 비교하면 대소문자 구분 없이 검색 가능

**예시**:
- `WHERE title LIKE '%Spring%'` → "Spring"만 찾음 (대소문자 구분)
- `WHERE LOWER(title) LIKE LOWER('%Spring%')` → "Spring", "SPRING", "spring" 모두 찾음

### 3. 검색어를 왜 trim()하나요?

**"사용자가 실수로 ' 스프링 ' (앞뒤 공백)을 입력하면 어떻게 될까요?"**

- 공백이 포함된 검색어는 정확한 결과를 찾지 못할 수 있습니다
- `trim()`으로 앞뒤 공백을 제거하여 정확한 검색 보장

**예시**:
- 검색어: " 스프링 " (앞뒤 공백)
- `trim()` 없이: `% 스프링 %` → 매칭 안 됨
- `trim()` 사용: `%스프링%` → 정확히 매칭

### 4. 검색어를 URL에 포함시키는 이유

**"검색 후 페이지를 이동하면 검색어가 사라지나요?"**

- `method="GET"`을 사용하면 검색어가 URL에 포함됩니다
- 예: `/board/list?page=2&keyword=스프링`
- 이렇게 하면:
  - 페이지 이동 시 검색어 유지
  - 북마크 가능
  - 뒤로 가기/앞으로 가기 가능

### 6. Mustache 템플릿에서 null 처리

**"검색어가 없을 때 템플릿에서 오류가 발생하는 이유는?"**

- Mustache 템플릿에서 변수가 `null`이거나 존재하지 않으면 오류가 발생할 수 있습니다
- 특히 `value="{{keyword}}"`처럼 직접 참조할 때 문제가 됩니다

**해결 방법**:
1. **Controller에서 null 체크**: `keyword != null ? keyword : ""`
2. **템플릿에서 조건부 렌더링**: `{{#keyword}}{{keyword}}{{/keyword}}`

**예시**:
```mustache
<!-- 오류 발생 가능 -->
value="{{keyword}}"

<!-- 안전한 방법 -->
value="{{#keyword}}{{keyword}}{{/keyword}}"
```

### 5. OR 조건의 의미

**"제목에 '스프링'이 있거나, 내용에 '스프링'이 있으면 모두 찾아주세요"**

- `WHERE title LIKE '%스프링%' OR content LIKE '%스프링%'`
- 제목 또는 내용 중 하나라도 검색어가 포함되면 조회
- 사용자가 더 많은 결과를 볼 수 있음

---

## 📊 전체 흐름도

```
사용자: "스프링" 검색
    ↓
Controller: keyword="스프링" 파라미터 받기
    ↓
Service: keyword가 있으므로 검색 쿼리 실행
    ↓
Repository: LIKE '%스프링%' 쿼리 실행
    ↓
Database: 제목 또는 내용에 "스프링"이 포함된 게시글 조회
    ↓
Service: 검색 결과를 PageDTO로 변환
    ↓
Controller: boardPage와 keyword를 Model에 추가
    ↓
View: 검색 결과 표시 + 검색어 유지
```

---

## 주요 파일 변경 목록

1. **BoardRepository.java**: 검색 쿼리 메서드 추가
   - `findByTitleContainingOrContentContaining()` 메서드

2. **BoardService.java**: 검색 로직 추가
   - `게시글목록조회()` 메서드에 `keyword` 파라미터 추가
   - 검색어 유무에 따른 조건부 분기

3. **BoardController.java**: 검색 파라미터 처리
   - `keyword` 파라미터 추가
   - 검색어를 Model에 추가 (null일 때 빈 문자열로 처리)

4. **list.mustache**: 검색 UI 추가
   - 검색 폼 추가 (`/board/list` 기준)
   - 페이징 링크에 검색어 유지 (`/board/list` 기준)
   - Mustache 조건부 렌더링으로 null 처리

---

## 테스트 시나리오

1. **기본 검색**: "스프링" 검색 → 제목 또는 내용에 "스프링"이 포함된 게시글만 표시
2. **대소문자 무시**: "SPRING", "spring", "Spring" 모두 동일한 결과
3. **부분 일치**: "스프링" 검색 → "스프링부트", "자바스프링" 등도 검색됨
4. **검색어 유지**: 검색 후 페이지 이동 → 검색어가 유지됨
5. **초기화**: 초기화 버튼 클릭 → 전체 목록으로 이동
6. **빈 검색어**: 검색어 없이 검색 → 전체 목록 표시
7. **공백 처리**: " 스프링 " (공백 포함) 검색 → 정상적으로 검색됨

---

## 🎯 핵심 정리

### 검색 기능의 3가지 핵심

1. **LIKE 검색**: 부분 일치 검색 (`%keyword%`)
2. **대소문자 무시**: `LOWER()` 함수 사용
3. **검색어 유지**: URL 파라미터로 검색어 전달

### 실무에서 중요한 점

- **성능**: 검색어가 없을 때는 전체 조회 쿼리 사용 (불필요한 검색 쿼리 방지)
- **사용자 경험**: 검색어 유지, 초기화 버튼 제공
- **안전성**: `trim()`으로 공백 제거, null 체크
- **템플릿 안전성**: Mustache 템플릿에서 null 변수 처리 (조건부 렌더링)
- **URL 구조**: `/board/list` 기준으로 통일 (홈 화면과 분리)

